import React, { FC } from 'react';
import { Card } from 'react-bootstrap';

export const MigratedConstructionEstimation: FC = () => {
    return (
        <Card className='p-3 text-center'>
            移行ツール経由で作成されたため、表示する情報はありません
        </Card>
    );
};
